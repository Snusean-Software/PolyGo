<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 x:Class="PolyGo.Views.Schedule.MainSchedulePage">
	<ContentPage.Content>
		<StackLayout>
			<Label x:Name="LabelNetworkError" 
						 IsVisible="False"
						 Text="Error: you don't have internet"
						 TextColor="OrangeRed"/>

			<Label x:Name="xxx" 
						 Text="{Binding Count}"
						 TextColor="CornflowerBlue"/>

			<CollectionView x:Name="collectionViewDays"
                    SelectionMode="Single">

				<CollectionView.ItemsLayout>
					<LinearItemsLayout Orientation="Vertical"
                               ItemSpacing="10" />
				</CollectionView.ItemsLayout>
				<CollectionView.ItemTemplate>
					<DataTemplate>
						<StackLayout>
							<Label Text="{Binding week_date_start}"/>
						</StackLayout>
					</DataTemplate>
				</CollectionView.ItemTemplate>
			</CollectionView>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>